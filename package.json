{
  "name": "hedera-hashie",
  "version": "0.0.1",
  "description": "A proof of participation token platform for Hedera",
  "scripts": {
    "build": "solcjs contracts/hashie.sol -o build --optimize --bin && solcjs contracts/hashie.sol -o build --abi",
    "deploy": "node scripts/deploy.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hashgraph/hethers": "^1.2.2",
    "@hashgraph/sdk": "^2.18.1",
    "dotenv": "^16.0.2"
  },
  "devDependencies": {}
}
